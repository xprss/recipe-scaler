server {
    listen 80;
    server_name localhost;

    client_max_body_size 20M;

    root /usr/share/nginx/html/recipe-scaler/browser;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
